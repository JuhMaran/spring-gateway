server:
  port: 8080

spring:
  application:
    name: spring-gateway

management:
  endpoint:
    health:
      probes:
        enabled: true
  health:
    readinessstate:
      enabled: true
    livenessstate:
      enabled: true

logging:
  level:
    root: debug
    org.springframework.cloud.gateway: trace